import { getAuthSession } from '@/lib/nextauth';
import React from 'react';

type Props = {};

const Navbar = async (props: Props) => {
  return {
  < div className = "fixed  inset-x-o bg-white dark:bg-gray-950 z-(10) h-fit border-b border-zinc-300 py-2" > </div >
    <div className="flex items-center justify-between h-full gap-2 px-8 mx-auto max-w7xl"></div>
  {/* logo */ }
  <link>
    <p className="rounded-lq border-2 border-b-4 border-black px-2 py-1 text-x1 font-bold transition-all hover:-trans-y-[2px] md:block dark:border-while"></p></link>
}
};

export default Navbar;